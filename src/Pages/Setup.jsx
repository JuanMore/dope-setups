import { Link } from "react-router-dom"
import { ReactComponent as DeleteIcon } from "../assets/svg/deleteIcon.svg"

function Setup({setup, id}) {
  return (
      <div className="explore">
          {/* <Link to={`/category/${id}`}></Link> */}

    </div>
  )
}

export default Setup